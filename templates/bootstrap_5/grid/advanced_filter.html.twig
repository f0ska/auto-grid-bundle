<span class="btn-group">
    {% if request.filter is defined and agRun(agIsAllowed, 'filter') %}
        <a class="btn btn-sm {{ btnClassActive }}" href="{{ agRun(agActionUrl, 'grid', {'filter': null}) }}">
            <i class="bi bi-funnel"></i>
        </a>
    {% endif %}
    {% if view.advancedFilterForm and agRun(agIsAllowed, 'advanced_filter') %}
        <button class="btn btn-sm {{ request.filter is defined ? btnClassActive : btnClass }}"
                data-bs-toggle="modal"
                data-bs-target="#adv{{ agId }}"
                data-bs-backdrop="false"
        ><i class="bi bi-search"></i></button>
    {% endif %}
</span>

{% if view.advancedFilterForm and agRun(agIsAllowed, 'advanced_filter') %}
    <div class="modal fade"
         id="adv{{ agId }}"
         tabindex="-1"
         aria-labelledby="adv{{ agId }}Label"
         aria-hidden="true"
         data-bs-backdrop="false"
    >
        <div class="modal-dialog modal-xl modal-fullscreen-sm-down">
            <div class="modal-content text-start">
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="adv{{ agId }}Label">
                        <i class="bi bi-search mr-1"></i>
                        {% if attributes.title is not empty %}
                            {{ attributes.title|trans }}
                        {% endif %}
                    </h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="container-fluid">
                        {% if view.formThemes is not empty %}
                            {% form_theme view.advancedFilterForm with view.formThemes %}
                        {% endif %}
                        {{ form_start(view.advancedFilterForm) }}
                        <div class="row">
                            {% for field in fields %}
                                {% if field.canFilter %}
                                    {% set formField = attribute(view.advancedFilterForm, field.name) %}
                                    {% if formField.children|length %}
                                        {% for child in formField.children %}
                                            <div class="col col-12 col-xl-4 col-lg-6 mb-3">
                                                <label class="form-label">{{ field_label(formField) }}
                                                    - {{ field_label(child) }}</label>
                                                {{ form_widget(child) }}
                                            </div>
                                        {% endfor %}
                                    {% else %}
                                        <div class="col col-12 col-xl-4 col-lg-6 mb-3">
                                            <label class="form-label">{{ field_label(formField) }}</label>
                                            {{ form_widget(formField) }}
                                        </div>
                                    {% endif %}
                                {% endif %}
                            {% endfor %}
                        </div>
                        {{ form_rest(view.advancedFilterForm) }}
                        {{ form_end(view.advancedFilterForm) }}
                    </div>
                </div>

                <div class="modal-footer">
                    <div class="container-fluid">
                        <div class="row">
                            <div class="col col-6">
                                <button type="submit"
                                        form="{{ view.advancedFilterForm.vars.attr.id }}"
                                        class="btn btn-outline-primary w-100 m-0"
                                >
                                    {{- 'f0ska.autogrid.button.submit'|trans -}}
                                </button>
                            </div>
                            <div class="col col-6">
                                <a class="btn btn-outline-secondary w-100 m-0 {% if request.filter is not defined %}disabled{% endif %}"
                                   href="{{ agRun(agActionUrl, 'grid', {'filter': null}) }}"
                                >
                                    {{- 'f0ska.autogrid.button.reset'|trans -}}
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endif %}
