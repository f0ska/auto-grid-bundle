{% block autogrid_field_date_content %}
    {% set value = agFieldValue(entity, field) %}
    {% if (value is null) %}
        -
    {% else %}
        {{ value.format(field.view.format) }}
    {% endif %}
{% endblock %}
