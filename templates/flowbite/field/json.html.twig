{% block autogrid_field_json_content %}
    {% if (value is null or value is same as '') %}
        -
    {% elseif (action is same as 'grid') %}
        {{ value|json_encode|ag_truncate(field.view.grid_truncate) }}
    {% else %}
        <pre class="bg-gray-50 p-2 rounded overflow-x-auto text-sm">{{ value|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
    {% endif %}
{% endblock %}
